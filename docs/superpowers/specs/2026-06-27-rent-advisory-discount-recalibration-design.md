# 租金降為參考 + 開價溢價門檻重構（投資 profile）

- 狀態：設計待審
- 日期：2026-06-27
- 範圍：investment profile 的篩選邏輯、行情估算來源、通知呈現
- 性質：純文件 + 一張參考表；零程式邏輯改動、零新測試（好時價為 agent 端互動查詢，非程式）

## 背景與問題

investment profile 原本用兩根支柱篩選物件：

1. **折扣**：`低於行情 >= 10%`，行情錨定實價登錄（成交價）。
2. **租金覆蓋率**：`租金覆蓋率 >= 1.0`（推薦）／`>= 0.8`（接近門檻），其中
   `租金覆蓋率 = 預估月租 / 月房貸`。

審視後發現兩個結構性缺陷：

### 缺陷 A：租金是 pipeline 裡最不可靠的輸入，卻有否決權

- 租金完全靠 agent 臨場估算，**無資料源、無快取、無新鮮度規則、不可重現**
  （對比走路距離有 ORS + MRT CSV + cache，行情有實價登錄錨點）。
- 一個 ±20% 都可能的點估計，被當成 `覆蓋率 >= 1.0` 的二元硬門檻 →
  物件在門檻兩邊隨雜訊跳動。**用拿不準的數字做決定性判斷**。

### 缺陷 B：折扣基準有系統性偏差（拿開價比成交價）

- 折扣公式 `(行情 − 物件單價) / 行情` 中，行情是**成交價**等級（實價登錄），
  物件單價卻是 iBigFun 上的**開價**（委託價），系統性偏高。
- 結果：幾乎每筆都算成「高於行情」（負折扣），`低於行情 >= 10%` 這個門檻在
  此基準下幾乎永不觸發；真正算出 `>= 10%` 的反而多是法拍/異常。**門檻壞了。**

## 設計決策（已與使用者確認）

1. **租金降為純參考**：只顯示、不影響分桶。不建實價登錄租金資料集；agent 粗估即可。
2. **指標改為「開價溢價」**：把折扣翻成溢價（開價相對成交行情高多少），沿溢價光譜重畫門檻。
3. **門檻錨定各市議價率**：用永慶房屋每季公布的成交議價率換算的典型溢價 `p*`。
4. **推薦採相對門檻**：`溢價 ≤ p*/2`（開價溢價明顯低於該市典型即推薦）。
5. **議價率以參考表維護**：每市一列、手動每季更新，比照 MRT CSV 的維護模式。
6. **行情錨定好時價 AVM（邊界物件）**：對接近門檻／有潛力的物件，agent 用好時價逐址
   估值當成交行情錨點；其餘物件維持現行實價登錄／樂居估法。比照現有「只對可疑／有潛力
   物件開詳情頁」的 bounded 模式。

被明確排除的選項：完全移除租金（保留量級訊號）、爬 591 即時租金（違背可重現）、
建實價登錄租金資料集（租金降為參考後不需要）、改動覆蓋率指標本身（改顯示現金流）、
好時價逆向內部 endpoint（ToS 風險、易壞）、好時價全量瀏覽器自動化（無頭不友善）、
國泰房地產指數議價率（屬預售／新成屋，屋型不符）。

## 指標定義

```
開價溢價 (premium) = (物件開價單價 − 成交行情單價) / 成交行情單價      # 越低越好；可為負
議價率 (r)         = 該市成交議價率 = (開價 − 成交) / 開價            # 永慶季報定義
典型溢價 (p*)      = r / (1 − r)                                      # 換算成「相對成交」的溢價
```

- 行情（成交行情單價）的取得見下節「行情估算」。溢價建立在該行情估算之上。
- 議價率分母是開價、溢價分母是成交，兩者不同；參考表須記錄此換算。

## 行情估算（更新）

溢價門檻的可靠度上限卡在「行情」估得準不準，因此本次強化行情來源。優先序：

1. **好時價 AVM（邊界物件優先）**：對接近門檻／有潛力的物件，agent 以好時價逐址估值
   （回傳單價 萬/坪 + 總價 萬）當成交行情錨點。涵蓋 19 縣市含目標都會區，免費。
2. 物件本身的實價登錄 / 樂居等可比成交（沿用現行做法），用於非邊界物件或好時價查不到時。
3. 弱／過期／跨站資料：物件**不可標推薦**（沿用既有資料品質規則）。

規則：

- **只查邊界物件**：好時價查詢限接近門檻或數字夠強值得驗證的物件，比照「只對可疑／有潛力
  物件開詳情頁」的 bounded 模式，控制成本；不對全量物件查。
- **不確定性**：好時價為單一估值、公布 MAPE ≈ 8–10%；agent 以此作為行情信心帶，
  溢價落在門檻邊界時於該物件註記行情信心。
- **接法**：agent 端互動查詢（非 enrich、非無頭自動化）。不逆向其內部 endpoint、
  不做全量瀏覽器自動化。好時價查不到的縣市／地址退回優先序第 2 項並註記。
- **來源可見**：每筆物件註記行情來源（好時價／實價登錄／樂居）與信心。

## 分桶門檻

| 分桶 | 門檻 | 台北示例（r=14% → p*≈16.3%） |
|---|---|---|
| **推薦** | `−10% < 溢價 ≤ p*/2` 且 走路可靠在內 且 乾淨 且 行情資料可靠不過期 | 溢價 ≤ ~8.1% |
| **接近門檻** | `p*/2 < 溢價 ≤ p*`，或溢價達推薦級但只差在行情待確認／走路待確認 | ~8.1% ~ ~16.3% |
| **排除** | `溢價 > p*` | > ~16.3% |
| **可疑/待查** | `溢價 ≤ −10%`（異常低，先驗證）＋ 既有可疑訊號（法拍/低資訊等） | 開價遠低於成交 |
| **前置排除** | 可靠步行 > 10 分（投資 profile 硬排除）— **不變** | — |

規則細節：

- 推薦是**相對**門檻：`p*` 取物件所在市的議價率換算值；不同市的推薦線不同。
- 溢價 `≤ −10%` 的異常低物件先進可疑/待查驗證，**不直接推薦**（多為法拍/資料異常）。
  驗證為乾淨且行情可靠後，可依其溢價歸入推薦/接近。
- 行情資料若過期/弱/跨站，物件**不可標推薦**，降到接近門檻並標人工確認（沿用既有資料品質規則）。
- 走路 `withinWalk === false`（可靠且 >10 分）仍前置排除；`withinWalk === null` 不排除，
  走 triage（不變）。

## 排序

- 推薦、接近門檻、排除三桶一律按 **開價溢價由低到高**（溢價越低越前），次鍵總價低者優先。
- 取代原本「折扣由高到低 / 覆蓋率由高到低」的排序。

## 租金（純參考）

- 報表顯示 `月租 ~{{estimated_rent}}（參考·低信心）` 與
  `現金流 ~{{monthly_cash_flow}}/月（參考）`（現金流 = 月租 − 房貸）。
- **拿掉覆蓋率**（`{{rent_coverage}}`）：既不卡關、又把融資假設混進篩選。
- 租金由 agent 粗估，標來源（若有）與低信心；**永不影響分桶或排序**。
- `scripts/lib/finance.ts` 的 `rentCoverage()` 保留為工具函式，但註解標明僅供參考顯示、
  不再用於門檻判斷。既有測試保留。

## 議價率參考表（新增）

- 新檔 `data/negotiation-rate.md`：每市一列。

  欄位：`city`（縣市名）、`rate`（成交議價率 r，%）、`source_quarter`（如 `2025Q3`）、
  `source_url`。檔頭記錄：`p* = r / (1 − r)`、永慶議價率分母為開價、每季更新流程。

- 初始種子（永慶 2025 Q3／2025 年 9 月，[自由電子報](https://estate.ltn.com.tw/article/25718)）：
  台北 14 / 台南 12.4 / 新北 12.2 / 高雄 11.8 / 桃園 10.8 / 新竹 10.8 / 台中 10.7。
  （取代較舊的 2025 Q1 版：台北 12.8 等。）
- 維護：每季永慶季報公布後手動更新 `rate` 與 `source_quarter`，比照 `taipei_mrt_exits.csv`
  的手動刷新模式。表中無對應市別時，agent 退而使用最接近的都會區或全國值，並於該物件註記。
- `data/README.md` 新增本表的說明段落。

## 不確定性與新鮮度處理

- 議價率帶 `source_quarter`，過舊（如超過 2 季）時於報表快速摘要標註「議價率資料偏舊」。
- 議價率隨景氣波動（永慶資料顯示熱市 7–10%、冷市 11–13%），故參數須定期校準，不寫死於程式。
- 溢價落在門檻邊界（如接近 `p*/2`）時，agent 在該物件註記其對行情估算的信心，避免邊界誤判。

## 動到的檔案

| 檔案 | 改動 |
|---|---|
| `docs/profiles/investment.md` | Criteria（溢價公式+門檻）、Estimation（租金參考化）、Report Buckets、排序/inline |
| `docs/reporting-rules.md` | 溢價公式與開價vs成交偏差說明、租金段落改參考、議價率參數用法、計算段落更新、行情估算優先序（好時價邊界物件錨點 + MAPE 信心帶） |
| `templates/investment-notify-template.md` | 標題列改溢價、財務列改租金/現金流參考且去覆蓋率、頂部門檻說明、排序、各桶空狀態文字 |
| `data/negotiation-rate.md`（新） | 各市議價率參考表 |
| `data/README.md` | 新增議價率表說明 |
| `scripts/lib/finance.ts` | 僅改註解，標 `rentCoverage` 為參考用途；無邏輯改動 |

## 不在本次範圍

- 不建實價登錄租金資料集、不爬 591 租金。
- 不改房貸計算、不改走路距離邏輯、不改可疑物件判斷流程。
- 不改 fetch/enrich 程式邏輯（行情與租金估算仍在 agent 端；好時價為 agent 端互動查詢）。
- 不做議價率的自動抓取或經驗校準自動化（手動季更新；經驗校準為日後可選項）。
- 好時價不逆向其內部 endpoint、不做全量瀏覽器自動化、不串接官方 API（無公開 API）；
  僅對邊界物件以 agent 端互動查詢取得逐址估值。

## 測試與驗收

- 純文件改動，`npm test` 應維持綠燈（`finance.test.ts` 不變）。
- 驗收以一次實際 investment 報表為準：確認推薦/接近依溢價門檻分桶、排序由溢價低到高、
  租金以參考形式呈現且不影響分桶、報表頂部門檻說明與議價率來源季別正確顯示。
